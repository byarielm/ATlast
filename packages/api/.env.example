# Database connection
DATABASE_URL=postgresql://atlast:localdev@localhost:5432/atlast

# OAuth configuration
# Get these from your Bluesky OAuth app
OAUTH_PRIVATE_KEY=

# Token Encryption (optional in development, required in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# TOKEN_ENCRYPTION_KEY=

# Server configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:5173

# Testing (optional)
# Run: pnpm test:login to get a session ID
# TEST_SESSION=
