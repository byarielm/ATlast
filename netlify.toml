[build]
  base = "/"
  command = "pnpm run build"
  functions = "packages/functions/src"
  publish = "packages/web/dist"

[dev]
  framework = "#custom"
  command = "npm run --prefix packages/web dev:full"
  targetPort = 5175
  port = 8888
  functionsPort = 9999
  autoLaunch = true

[[redirects]]
  from = "/oauth-client-metadata.json"
  to = "/.netlify/functions/client-metadata"
  status = 200

[[redirects]]
  from = "/oauth/callback"
  to = "/.netlify/functions/oauth-callback"
  status = 200

[[headers]]
  for = "/oauth-client-metadata.json"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Cache-Control = "public, max-age=3600"

[[headers]]
    for = "/.well-known/*"
    [headers.values]
    Access-Control-Allow-Origin = "*"

[[headers]]
    for = "/*"
    [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"
    # CSP relaxed for dev mode - script-src includes 'unsafe-eval' for Vite HMR
    # In production, Netlify will use build context to apply stricter CSP
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' ws: wss: http://localhost:* http://127.0.0.1:* https://*.bsky.app https://*.bsky.network https://public.api.bsky.app; frame-ancestors 'none'; base-uri 'self'; form-action 'self';"
