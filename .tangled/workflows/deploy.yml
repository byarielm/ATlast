engine: nixery
when:
  - event: ["push"]
    branch: ["master"]

clone:
  skip: true

dependencies:
  nixpkgs:
    - curl

steps:
  - name: deploy
    command: |
      curl -X POST -d '{}' $NETLIFY_WEBHOOK_URL